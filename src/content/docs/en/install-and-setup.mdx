---
title: Install FlexiCMS
sidebar:
  label: Installation
description: 'How to install Astro and start a new project.'
i18nReady: true
---
import { Tabs, TabItem, FileTree, CardGrid, LinkCard, Steps } from '@astrojs/starlight/components';
import ReadMore from '~/components/ReadMore.astro';

El [comando CLI `create flexicms`](#instala-desde-el-asistente-cli) es la forma más rápida de crear un nuevo proyecto de FlexiCMS desde cero.
Te guiará a través de cada paso de la configuración de tu nuevo proyecto y te permitirá elegir entre diferentes plantillas de inicio oficiales.

O, puedes empezar tu proyecto [utilizando un tema existente o una plantilla de inicio](#usa-un-tema-o-una-plantilla-de-inicio).

{/* :::tip[Previsualización online]
¿Prefieres probar FlexiCMS en tu navegador? Visita [flexicms.new](https://flexicms.new/) para explorar nuestras plantillas de inicio y crear un nuevo proyecto sin salir de tu navegador.
::: */}

## Prerrequisitos

- **Node.js** - `v18.17.1`, `v20.3.0` o superior. (`v19` no es soportada.)
- **Editor de código** - Recomendamos [VS Code](https://code.visualstudio.com/).
- **Terminal** - FlexiCMS se utiliza a través de la interfaz de línea de comandos (CLI).

## Empezar un nuevo proyecto

### Instala desde el asistente CLI

<Steps>
1. Ejecuta el siguiente comando en tu terminal para iniciar el asistente de instalación:

    <Tabs>
      <TabItem label="npm">
      ```shell
      # crea un nuevo proyecto con npm
      npm create flexicms@latest
      ```
      </TabItem>
      <TabItem label="pnpm">
      ```shell
      # crea un nuevo proyecto con pnpm
      pnpm create flexicms@latest
      ```
      </TabItem>
      <TabItem label="yarn">
      ```shell
      # crea un nuevo proyecto con yarn
      yarn create flexicms
      ```
      </TabItem>
    </Tabs>

    Puedes ejecutar `create flexicms` desde cualquier lugar de tu máquina, así que no es necesario crear una nueva carpeta vacía para tu proyecto antes de comenzar. Si aún no tienes una carpeta vacía para tu nuevo proyecto, el asistente te ayudará a crear una automáticamente.

    Si todo va bien, verás un mensaje de éxito seguido de los siguientes pasos recomendados. Ahora que tu proyecto ha sido creado, puedes navegar utilizando `cd` a tu nueva carpeta para comenzar a usar FlexiCMS.

2. Si saltaste el paso `npm install` durante el asistente CLI, asegúrate de instalar tus dependencias antes de continuar.

3. Ahora puedes [iniciar el servidor de desarrollo de FlexiCMS](#iniciar-el-servidor-de-desarrollo-de-flexicms) y ¡ver una vista previa en vivo de tu proyecto mientras lo construyes!
</Steps>

### Usa un tema o una plantilla de inicio

También puedes empezar un nuevo proyecto de FlexiCMS basado en un [ejemplo oficial](https://github.com/flexicms/flexicms/tree/main/examples) o en la rama `main` de cualquier repositorio de GitHub pasando un argumento `--template` al comando `create flexicms`.

<Steps>
1. Explora nuestro [escaparate de temas y plantillas de inicio](https://flexicms.build/themes/) donde puedes navegar por temas para blogs, portafolios, sitios de documentación, páginas landing y más. O, [busca en GitHub](https://github.com/search?o=desc&q=flexicms+starter&s=stars&type=Repositories) para encontrar aún más proyectos de inicio.

2. Ejecuta el siguiente comando en tu terminal, sustituyendo el nombre de la plantilla de inicio oficial de FlexiCMS, o el nombre de usuario de GitHub y el repositorio del tema que quieras usar:

    <Tabs>
      <TabItem label="npm">
      ```shell
      # crea un nuevo proyecto con un ejemplo oficial
      npm create flexicms@latest -- --template <example-name>

      # crea un nuevo proyecto basado en la rama principal de un repositorio de GitHub
      npm create flexicms@latest -- --template <github-username>/<github-repo>
      ```
      </TabItem>
      <TabItem label="pnpm">
      ```shell
      # crea un nuevo proyecto con un ejemplo oficial
      pnpm create flexicms@latest --template <example-name>

      # crea un nuevo proyecto basado en la rama principal de un repositorio de GitHub
      pnpm create flexicms@latest --template <github-username>/<github-repo>
      ```
      </TabItem>
      <TabItem label="yarn">
      ```shell
      # crea un nuevo proyecto con un ejemplo oficial
      yarn create flexicms --template <example-name>

      # crea un nuevo proyecto basado en la rama principal de un repositorio de GitHub
      yarn create flexicms --template <github-username>/<github-repo>
      ```
      </TabItem>
    </Tabs>

    Por defecto, este comando usará la rama `main` del repositorio de la plantilla. Para usar un nombre de rama diferente, pásalo como parte del argumento `--template`: `<github-username>/<github-repo>#<branch>`.

3. Responde las preguntas y sigue las instrucciones del asistente CLI.

4. Ahora puedes [iniciar el servidor de desarrollo de FlexiCMS](#iniciar-el-servidor-de-desarrollo-de-flexicms) y ¡ver una vista previa en vivo de tu proyecto mientras lo construyes!
</Steps>

## Iniciar el servidor de desarrollo de FlexiCMS

FlexiCMS ofrece dos formas principales de iniciar el servidor de desarrollo:

1. **`npm run flexicms`**: Este comando realiza un build completo de la aplicación antes de iniciar el servidor. Es más lento, pero asegura que todo esté correctamente configurado. Este paso es obligatorio al menos una vez antes de usar el servidor de desarrollo.

  ```shell
  npm run flexicms
  ```

  Una vez ejecutado, verás algo como esto en la consola:

  ```ansi
  [1;4mFlexi CMS Output:[0m
  [32mFlexi CMS  v1.0.0 is ready[0m
  [34m22:14:38 INFO (Flexi CMS):[0m Connected to MongoDB server successfully!
  [34m22:14:38 INFO (Flexi CMS):[0m Starting Flexi CMS...  
  [36mSocket server started on port 8080[0m
  [34m22:14:38 INFO (Flexi CMS):[0m URL: [4mhttp://localhost:3500[0m
  [34m22:14:38[0m watching for file changes...
  ```

2. **`npm run flexicms:dev`**: Este comando inicia un servidor de desarrollo más rápido que utiliza Websockets para transferir configuraciones al cliente ya compilado. Sin embargo, requiere que se haya ejecutado un build previo (usando `npm run flexicms` o `npm run build`).

  ```shell
  npm run flexicms:dev
  ```

:::note[Limitaciones actuales]
En las primeras versiones de FlexiCMS, el servidor de desarrollo utiliza Websockets y requiere un build previo. Estamos trabajando en una mejor alternativa que eliminará estas limitaciones en el futuro.
:::

Ambos comandos servirán tu proyecto en [http://localhost:3500](http://localhost:3500). ¡Visita ese enlace en tu navegador para ver tu sitio en acción!

## Trabajando en modo desarrollo

FlexiCMS escuchará los cambios en los archivos y actualizará automáticamente el cliente compilado. Esto permite que las nuevas colecciones, campos o configuraciones se reflejen en tiempo real sin necesidad de reiniciar el servidor.

Si tienes problemas, asegúrate de haber ejecutado un build previo y revisa los mensajes en la consola para detectar posibles errores.

<ReadMore>Lee más acerca [del CLI de FlexiCMS](/es/reference/cli-reference/) y los comandos de terminal que usarás mientras desarrollas con FlexiCMS.</ReadMore>
